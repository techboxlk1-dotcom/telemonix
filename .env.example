# TeleMonix env vars — paste into Netlify → Site Settings → Environment Variables
# (also work for self-hosted / GitHub Actions builds)

# === Public (safe in client bundle, prefixed with VITE_) ===
VITE_SUPABASE_URL=https://ouxtjwgpttsqbbbvvfeo.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_-PDPqYz5dXtorTazvUW3cA_ZUaWpErT
VITE_SUPABASE_PROJECT_ID=ouxtjwgpttsqbbbvvfeo

# === Server-only (NEVER expose to client) ===
SUPABASE_URL=https://ouxtjwgpttsqbbbvvfeo.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_-PDPqYz5dXtorTazvUW3cA_ZUaWpErT
# Service role key — get from Lovable Cloud (Project Settings → Backend → Secrets).
# Required for server functions, cron, webhook, click tracking.
SUPABASE_SERVICE_ROLE_KEY=

# Telegram bot token — already stored in Lovable Cloud Secrets.
TELEGRAM_BOT_TOKEN=

# Lovable AI Gateway key — auto-generated. Required for Smart CTA AI suggest.
LOVABLE_API_KEY=
